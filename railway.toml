[build]
builder = "nixpacks"
buildCommand = "npm ci --legacy-peer-deps"

[deploy]
startCommand = "npx tsx src/jobs/worker.ts"
healthcheckPath = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
